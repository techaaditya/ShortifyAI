/**
 * Captions Index
 * 
 * Central export for caption system
 */

// Styles and utilities
export {
    CAPTION_PRESETS,
    CAPTION_ANIMATIONS,
    FONT_OPTIONS,
    getPresetStyle,
    mergeStyles,
    generateCaptionCSS,
    splitIntoSegments,
    formatTimestamp,
    type CaptionWord,
    type CaptionSegment,
    type CaptionStyle,
    type CaptionPreset,
    type CaptionPosition,
    type CaptionAnimation,
} from './caption-styles';
